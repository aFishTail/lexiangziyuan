# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True

# Database settings - SQLite (默认)
# DATABASE_ENGINE=django.db.backends.sqlite3
# DATABASE_NAME=db.sqlite3

# Database settings - MySQL
DATABASE_ENGINE=django.db.backends.mysql
DATABASE_NAME=lenjoy_db
DATABASE_USER=lenjoy_user
DATABASE_PASSWORD=your_password_here
DATABASE_HOST=localhost
DATABASE_PORT=3306

# 使用sqlite，用于本地调试
USE_SQLITE = False

# Database settings - PostgreSQL
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=db_name
# DATABASE_USER=db_user
# DATABASE_PASSWORD=db_password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# Email settings
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password
EMAIL_USE_TLS=True

# JWT settings
JWT_ACCESS_TOKEN_LIFETIME_DAYS=30
JWT_REFRESH_TOKEN_LIFETIME_DAYS=365

# CORS settings
CORS_ALLOW_ALL_ORIGINS=True
# 对于生产环境，建议使用以下设置方式（以逗号分隔多个域名）
# CORS_ALLOW_ALL_ORIGINS=False
# CORS_ALLOWED_ORIGINS=https://example.com,https://sub.example.com,http://localhost:8080
CORS_ALLOW_CREDENTIALS=True

# Other sensitive information
# Add any API keys or other sensitive data here
# STRIPE_API_KEY=your-stripe-api-key
# ALIPAY_API_KEY=your-alipay-api-key
