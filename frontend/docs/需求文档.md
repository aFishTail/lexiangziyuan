# Lenjoy 资源分享站 - 产品需求文档 (PRD)

> **版本**: v1.0  
> **更新日期**: 2025-10-28  
> **文档状态**: 正式版

---

## 📋 目录

1. [项目概述](#1-项目概述)
2. [技术架构](#2-技术架构)
3. [功能范围说明](#3-功能范围说明)
4. [页面结构与功能详情](#4-页面结构与功能详情)
5. [数据模型](#5-数据模型)
6. [API 接口规范](#6-api-接口规范)
7. [SEO 与性能优化](#7-seo-与性能优化)
8. [移动端适配](#8-移动端适配)
9. [开发优先级](#9-开发优先级)

---

## 1. 项目概述

### 1.1 产品定位
Lenjoy 是一个**资源分享和资源网站导航平台**，旨在为用户提供：
- **资源信息集合**：精选的高质量资源内容，支持快速搜索和浏览
- **网站资源导航**：收纳优质网站工具，按分类组织展示

### 1.2 核心价值
- 汇聚精选知识与实用工具，让优质资源触手可及
- 通过精心策展，帮助用户节省时间，快速找到优质资源
- 简洁高效的浏览体验，降低使用门槛，无需注册即可访问

### 1.3 目标用户
- **知识探索者**：寻找特定领域知识、行业洞察和实践经验的学习者
- **效率提升者**：需要工具导航和资源推荐的各类工作者（设计师、开发者、产品经理、运营、内容创作者等）
- **资源发现者**：希望持续发现优质内容和网站的互联网用户

---

## 2. 技术架构

### 2.1 前端技术栈
- **框架**: Next.js 15 + React 19 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS 4 + shadcn/ui 组件库
- **状态管理**: @tanstack/react-query (服务端数据缓存)
- **主题**: next-themes (暗/亮主题切换)

### 2.2 后端技术栈
- **框架**: Django REST Framework
- **数据库**: PostgreSQL/MySQL
- **管理后台**: Django Admin (内置，不需要自定义后台)

### 2.3 部署架构
- **前端**: Vercel / 自托管服务器
- **后端**: Django 服务器部署
- **API 通信**: RESTful API (通过 `NEXT_PUBLIC_API_BASE_URL` 配置)

---

## 3. 功能范围说明

### 3.1 资源功能 ✅

| 功能         | 是否支持 | 说明                               |
| ------------ | -------- | ---------------------------------- |
| 资源列表展示 | ✅        | 支持分页，每页12篇                 |
| 资源详情页   | ✅        | 包含正文、作者、日期、阅读量       |
| 资源分类     | ✅        | 在列表页支持筛选，不需要独立聚合页 |
| 资源标签     | ✅        | 在列表页支持筛选，不需要独立聚合页 |
| 阅读量统计   | ✅        | 后端统计，前端展示                 |
| 相关资源推荐 | ✅        | 基于分类/标签推荐                  |
| 热门资源推荐 | ✅        | 基于浏览量排序                     |
| 评论功能     | ❌        | 暂时不需要                         |

### 3.2 资源网站功能 ✅

| 功能         | 是否支持 | 说明                                |
| ------------ | -------- | ----------------------------------- |
| 资源列表展示 | ✅        | 不分页，一页展示全部（每分类≤30个） |
| 资源分类     | ✅        | 按分类组织展示                      |
| 资源标签     | ❌        | 资源网站没有标签                    |
| 资源详情页   | ❌        | 点击卡片直接跳转外部网站            |
| 点击量统计   | ✅        | 后端统计，前端展示                  |
| 推荐标记     | ✅        | 管理员在后台设置                    |
| 热门标记     | ✅        | 根据点击量自动计算                  |

### 3.3 搜索功能 ✅

| 功能          | 是否支持 | 说明                 |
| ------------- | -------- | -------------------- |
| 资源搜索      | ✅        | 搜索标题、摘要、正文 |
| 资源搜索      | ❌        | 目前只支持资源搜索   |
| 分类/标签筛选 | ❌        | 搜索不支持筛选       |
| 搜索历史记录  | ✅        | 本地存储，前端实现   |
| 热门搜索词    | ✅        | 后端提供数据         |

### 3.4 首页展示 ✅

| 模块         | 展示数量 | 排序规则       |
| ------------ | -------- | -------------- |
| 最新资源     | 后台计算 | 按发布时间倒序 |
| 热门资源     | 后台计算 | 按浏览量倒序   |
| 热门资源网站 | 后台计算 | 按点击量倒序   |
| 分类快速入口 | 全部显示 | -              |

### 3.5 用户功能 ❌

| 功能          | 是否支持 |
| ------------- | -------- |
| 用户注册/登录 | ❌ 不需要 |
| 用户收藏      | ❌ 不需要 |
| 用户反馈      | ❌ 不需要 |
| 评论系统      | ❌ 不需要 |

### 3.6 其他功能 ✅

| 功能       | 是否支持 | 说明                 |
| ---------- | -------- | -------------------- |
| 移动端适配 | ✅        | 响应式设计           |
| 暗/亮主题  | ✅        | next-themes 实现     |
| 网站地图   | ✅        | sitemap.xml          |
| SEO 优化   | ✅        | meta标签、结构化数据 |
| 管理后台   | ❌        | Django Admin 够用    |

---

## 4. 页面结构与功能详情

### 4.1 首页 `/`

#### 页面结构
```
┌─────────────────────────────────────┐
│         网站头部 (Header)            │
│  Logo · 首页 · 资源 · 资源 · 搜索 · 主题 │
├─────────────────────────────────────┤
│         Hero 区域                    │
│  - 品牌标语与价值主张                 │
│  - CTA按钮（浏览资源/探索资源）        │
│  - 快速导航卡片（资源/资源分类）       │
│  - 统计数据展示                      │
├─────────────────────────────────────┤
│         最新资源 (3篇)               │
│  - ArticleCard 卡片组件              │
│  - 查看全部资源按钮                  │
├─────────────────────────────────────┤
│         热门资源 (6篇)               │
│  - ArticleCard 卡片组件              │
│  - 按浏览量排序                      │
│  - 查看更多按钮                      │
├─────────────────────────────────────┤
│         热门资源网站 (6个)            │
│  - websiteCard 卡片组件             │
│  - 按点击量排序                      │
│  - 浏览全部资源按钮                  │
├─────────────────────────────────────┤
│         CTA 区域                     │
│  - 引导用户使用搜索功能              │
├─────────────────────────────────────┤
│         网站底部 (Footer)            │
│  - 快速链接 · 版权信息 · 网站地图     │
└─────────────────────────────────────┘
```

#### 数据需求
- **最新资源**: 3篇，按发布时间倒序
- **热门资源**: 6篇，按浏览量倒序
- **热门资源**: 6个，按点击量倒序
- **资源分类**: 全部分类（用于快速导航）
- **资源分类**: 全部分类（用于快速导航）

---

### 4.2 资源列表页 `/articles`

#### 页面结构
```
┌─────────────────────────────────────┐
│         页面标题 + 描述              │
├─────────────────────────────────────┤
│         筛选器 (Filters)             │
│  - 分类下拉选择（单选）              │
│  - 标签多选 Pills                   │
│  - 重置按钮                          │
├─────────────────────────────────────┤
│         资源列表                     │
│  - ArticleCard 网格布局              │
│  - 封面图、标题、摘要                │
│  - 分类、标签、作者、日期、阅读量     │
├─────────────────────────────────────┤
│         分页导航 (Pagination)        │
│  - 上一页/下一页 · 页码显示          │
└─────────────────────────────────────┘
```

#### 功能需求
✅ **需要分页**
- 每页显示数量：12篇资源
- 支持页码跳转

✅ **支持筛选**（在列表页实现，不需要独立聚合页）
- 按分类筛选（单选下拉菜单）
- 按标签筛选（多选 Tag Pills）
- 支持同时按分类+标签筛选
- 重置筛选条件按钮

✅ **排序选项**
- 默认：按发布时间倒序
- 可选：按阅读量倒序（热门优先）

✅ **URL 参数**
- `?page=1` - 页码
- `?category=1` - 分类 ID
- `?tags=1,2,3` - 标签 ID（多个用逗号分隔）

#### 交互细节
- 筛选器变化时自动重新加载列表
- 保持筛选条件在 URL 中，支持分享链接
- 空状态提示（无资源时显示友好提示）
- 加载状态（骨架屏或 loading spinner）

---

### 4.3 资源详情页 `/articles/[id]`

#### 页面结构
```
┌─────────────────────────────────────┐
│         面包屑导航                   │
│  首页 > 资源 > 分类名 > 资源标题     │
├─────────────────────────────────────┤
│         资源头部                     │
│  - 标题                             │
│  - 分类徽章                         │
│  - 作者 · 发布日期 · 阅读量         │
│  - 标签 Pills                       │
├─────────────────────────────────────┤
│         资源正文                     │
│  - 富文本渲染                  │
│  - 代码高亮                         │
│  - 图片优化                         │
│  - 标题锚点（自动生成目录）          │
├─────────────────────────────────────┤
│         相关资源推荐                 │
│  - 同分类/同标签资源                │
│  - 3-6篇资源卡片                    │
├─────────────────────────────────────┤
│         热门资源推荐                 │
│  - 按浏览量排序                     │
│  - 3-6篇资源卡片                    │
└─────────────────────────────────────┘
```

#### 功能需求

✅ **必需字段**
1. **正文** (content): 富文本 格式的资源内容
2. **发布日期** (published_at): 资源发布时间
3. **阅读量** (view_count): 浏览次数统计

✅ **相关资源推荐**
- 推荐逻辑：
  1. 优先推荐同分类资源
  2. 其次推荐有共同标签的资源
  3. 排除当前资源
- 显示数量：3-6篇

✅ **热门资源推荐**
- 排序依据：按浏览量倒序
- 排除当前资源
- 显示数量：3-6篇

✅ **阅读量统计**
- 页面加载时调用 API 增加浏览量
- 防止重复计数（可使用 session 或 cookie）

❌ **暂不支持**
- 评论功能
- 点赞/收藏功能
- 社交分享按钮（可后续添加）

---

### 4.4 资源网站列表页 `/websites`

#### 页面结构
```
┌─────────────────────────────────────┐
│         页面标题 + 描述              │
├─────────────────────────────────────┤
│         分类导航 (Tabs/Pills)        │
│  全部 · 设计工具 · 开发工具 · AI工具 │
├─────────────────────────────────────┤
│         资源卡片网格                 │
│  - WebsiteCard 组件                 │
│  - Logo展示                         │
│  - 名称 + 简介                      │
│  - 分类徽章                         │
│  - 推荐/热门标记                    │
│  - 点击量显示                       │
│  - 访问按钮                         │
└─────────────────────────────────────┘
```

#### 功能需求

❌ **不需要分页**
- 每个分类下资源数量：个位数到30个以内
- 单页展示所有资源（使用分类 Tab 切换）

❌ **资源网站没有标签**
- 只有分类，没有标签字段

❌ **不需要资源详情页**
- 点击资源卡片直接在新标签页打开外部网站
- 使用 `target="_blank" rel="noopener noreferrer"`

✅ **推荐标记**
- 管理员在 Django Admin 后台设置 `is_recommended=True`
- 前端显示"推荐"徽章（如⭐星标或特殊样式）

✅ **热门标记**
- 后端根据点击量自动计算 `is_hot` 字段
- 前端显示"热门"徽章（如🔥火焰图标）
- 计算规则示例：点击量 > 1000 或排名前10%

✅ **点击量统计**
- 每次点击资源卡片时调用 API 记录点击
- 前端显示点击量数字

✅ **排序规则**
1. 推荐资源优先显示（`is_recommended=True`）
2. 其次按点击量倒序
3. 最后按添加时间倒序

#### 交互细节
- 点击分类 Tab 切换，显示对应分类的资源
- 点击资源卡片时：
  1. 调用 API 增加点击量
  2. 在新标签页打开外部网站
- 加载状态处理

---

### 4.5 搜索页 `/search`

#### 页面结构
```
┌─────────────────────────────────────┐
│         搜索框                       │
│  - 输入框（自动聚焦）                │
│  - 搜索按钮                         │
├─────────────────────────────────────┤
│         搜索建议区（输入前/无结果时） │
│  - 搜索历史记录（本地存储）          │
│  - 热门搜索词（后端提供）            │
├─────────────────────────────────────┤
│         搜索结果（输入后）            │
│  - 结果数量统计                     │
│  - ArticleCard 网格布局              │           │
│  - 加载状态                         │
│  - 空状态提示                       │
│  - 分页导航                         │
└─────────────────────────────────────┘
```

#### 功能需求

✅ **仅搜索资源**
- 搜索范围：标题
- 不包含资源网站搜索

❌ **不支持高级筛选**
- 不支持按分类筛选
- 不支持按标签筛选
- 只有纯文本关键词搜索

✅ **搜索历史记录**（前端实现）
- 使用 localStorage 存储
- 保存最近10条搜索记录
- 支持删除单条记录
- 支持清空全部历史
- 点击历史记录快速搜索

✅ **热门搜索词**（后端提供）
- 后端统计搜索频次
- 返回前10个热门搜索词
- 点击热门词快速搜索

#### 交互细节
- 实时搜索：输入后防抖500ms自动搜索
- 关键词高亮：搜索结果中高亮显示匹配的关键词（可选）
- 分页：支持，每页12篇
- 空状态：无结果时显示搜索建议和热门搜索词

---

### 4.6 网站头部 (Header)

#### 组件结构
```
桌面端：
┌─────────────────────────────────────┐
│ Logo  首页  资源  资源  [搜索🔍]  [主题🌙] │
└─────────────────────────────────────┘

移动端：
┌─────────────────────────────────────┐
│ [☰菜单]  Logo          [搜索🔍]  [主题🌙] │
└─────────────────────────────────────┘
```

#### 功能需求
- **Logo**: 点击返回首页
- **导航菜单**: 
  - 首页 `/`
  - 资源 `/articles`
  - 资源 `/websites`
- **搜索图标**: 点击跳转搜索页 `/search`
- **主题切换**: 暗/亮主题切换按钮
- **移动端**: 
  - 汉堡菜单（左上角）
  - 侧边抽屉导航（Sheet 组件）
  - 保留搜索和主题按钮

---

### 4.9 网站底部 (Footer)

#### 组件结构
```
┌─────────────────────────────────────┐
│         快速链接                     │
│  首页 · 资源 · 资源 · 搜索           │
├─────────────────────────────────────┤
│         网站信息                     │
│  © 2025 Lenjoy. All rights reserved.│
│  网站地图                            │
|  免责声明                            │
|  联系方式                            │   
└─────────────────────────────────────┘
```

#### 功能需求
- 快速链接：主要页面导航
- 版权信息：年份自动更新
- 网站地图链接：指向 `/sitemap.xml`
- 联系方式：WX : 微信号
- 免责声明：本站资源收集来源网络和用户投稿，请勿用于商业行为，如有侵权请及时联系删除

---

## 5. 数据模型

### 5.1 资源 (Article)

```typescript
interface Article {
  id: number;                     // 主键ID（用于URL）
  name: string;                   // 标题
  content: string;                // 正文（Markdown 格式）
  cover_img?: string;             // 封面图 URL（可选）
  view_count: number;             // 阅读量 ✅ 必需
  category: Category;             // 所属分类
  tags: Tag[];                    // 标签列表
  source: string;                 // 来源
  remark: string;                 // 备注
  status: number;                 // 状态 (0=待审核, 1=已发布, 3=已下架)
  created_time: string;           // 创建时间（作为发布时间使用）
  update_time: string;            // 更新时间
}

interface ArticleSummary {
  id: number;
  name: string;
  cover_img?: string;
  view_count: number;             // ✅ 必需
  category: Category;
  tags: Tag[];
  created_time: string;           // 作为发布时间使用
  // ❌ 不需要 summary，前端根据 content 截取
  // ❌ 不需要 author，暂时不实现
  // ❌ 不需要 slug，直接使用 id
}
```

### 5.2 资源网站 (ResourceWebsite)

```typescript
interface ResourceWebsite {
  id: number;                     // 主键ID（用于URL）
  name: string;                   // 资源名称
  description: string;            // 简介
  url: string;                    // 外部网站链接 ✅ 必需
  logo?: string;                  // Logo URL（可选）
  category: ResourceCategory;     // 所属分类（独立的资源分类）
  visit_count: number;            // 访问次数（对应需求的 click_count）✅ 必需
  is_featured: boolean;           // 是否推荐（对应需求的 is_recommended）✅ 管理员设置
  status: number;                 // 状态 (0=待审核, 1=已发布, 2=已禁用)
  remark: string;                 // 备注
  created_time: string;           // 创建时间
  update_time: string;            // 更新时间
  // ❌ 没有 tags 字段，资源网站没有标签
  // ❌ 不需要 slug，直接使用 id
  // ❌ 不需要 is_hot 字段，通过接口根据 visit_count 动态返回热门资源
}
```

### 5.3 分类 (Category)

**说明**：资源分类和资源分类使用独立的模型，不合并

```typescript
// 资源分类
interface ArticleCategory {
  id: number;
  name: string;                   // 分类名称
  description?: string;           // 分类描述（可选）
  order: number;                  // 排序顺序
  article_count?: number;         // 资源数量（API 返回时计算）
}

// 资源分类
interface ResourceCategory {
  id: number;
  name: string;                   // 分类名称
  description?: string;           // 分类描述（可选）
  order: number;                  // 排序顺序
  icon?: string;                  // 图标
  resource_count?: number;        // 资源数量（API 返回时计算）
}

// ❌ 不需要 slug，直接使用 id
// ❌ 不需要 type 字段，保持两个独立的分类模型
```

### 5.4 标签 (Tag)

```typescript
interface Tag {
  id: number;                     // 主键ID
  name: string;                   // 标签名称
  synonyms: string[];             // 同义词列表
  article_count?: number;         // 使用该标签的资源数（API 返回时计算）
  // ❌ 标签只用于资源，资源网站没有标签
  // ❌ 不需要 slug，直接使用 id
}
```

### 5.5 搜索历史 (SearchHistory) - 前端本地存储

```typescript
interface SearchHistory {
  keyword: string;                // 搜索关键词
  timestamp: number;              // 搜索时间戳
}

// localStorage key: 'lenjoy_search_history'
// 最多存储 10 条记录
```

---

## 6. API 接口规范

### 6.1 基础配置
- **Base URL**: `process.env.NEXT_PUBLIC_API_BASE_URL`
- **数据格式**: JSON
- **统一响应格式**:

```typescript
interface ApiResponse<T> {
  data: T;
  message?: string;
  error?: string;
}

interface PaginatedResponse<T> {
  data: T[];
  pagination: {
    page: number;           // 当前页码
    page_size: number;      // 每页数量
    total: number;          // 总记录数
    total_pages: number;    // 总页数
  };
}
```

---