# 本地开发指南 🛠️

本文档介绍如何使用 **混合模式** 进行本地开发：数据库运行在 Docker 中，前后端在本地直接运行，享受完整的热重载体验。

## 📋 环境要求

- **Python** 3.12+
- **Node.js** 20+
- **pnpm** (推荐) 或 npm
- **Docker** 20.10+
- **Docker Compose** 2.0+

## 🚀 快速开始

### 1. 启动数据库

```bash
# 只启动 MySQL 数据库服务
docker compose up -d db

# 查看数据库状态（等待 healthy 状态）
docker compose ps

# 查看数据库日志
docker compose logs -f db
```

> 💡 数据库默认配置：
> - 主机: `localhost`
> - 端口: `3306`
> - 数据库名: `lenjoy`
> - 用户名: `lenjoy`
> - 密码: `lenjoy123`

### 2. 启动后端 (Django)

```powershell
# 进入后端目录
cd api

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境 (Windows PowerShell)
.\venv\Scripts\Activate.ps1

# 激活虚拟环境 (Windows CMD)
# .\venv\Scripts\activate.bat

# 激活虚拟环境 (Linux/macOS)
# source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

**配置环境变量：**

在 `api` 目录下创建 `.env` 文件：

```env
DEBUG=True
SECRET_KEY=dev-secret-key-for-local-development

# 数据库配置（连接 Docker 中的 MySQL）
DATABASE_ENGINE=django.db.backends.mysql
DATABASE_NAME=lenjoy
DATABASE_USER=lenjoy
DATABASE_PASSWORD=lenjoy123
DATABASE_HOST=localhost
DATABASE_PORT=3306

# CORS 配置
CORS_ALLOW_ALL_ORIGINS=True
ALLOWED_HOSTS=*
```

**运行开发服务器：**

```powershell
# 执行数据库迁移
python manage.py migrate

# 创建超级用户（首次运行）
python manage.py createsuperuser

# 启动开发服务器
python manage.py runserver 8000
```

后端访问地址：
- API: http://localhost:8000/api
- 管理后台: http://localhost:8000/admin
- Swagger 文档: http://localhost:8000/swagger

### 3. 启动前端 (Next.js)

打开新的终端窗口：

```powershell
# 进入前端目录
cd frontend

# 安装依赖
pnpm install

# 或使用 npm
# npm install
```

**配置环境变量：**

在 `frontend` 目录下创建 `.env.local` 文件：

```env
# API 地址（直接连接本地后端）
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_CLIENT_API_URL=http://localhost:8000
```

**运行开发服务器：**

```powershell
# 启动开发服务器（带热重载）
pnpm dev

# 或使用 npm
# npm run dev
```

前端访问地址：http://localhost:3000

## 📁 项目结构

```
lxiangziyuan/
├── api/                    # Django 后端
│   ├── article/            # 文章模块
│   ├── category/           # 分类模块
│   ├── config/             # Django 配置
│   ├── common/             # 公共组件
│   ├── resource_website/   # 资源网站模块
│   ├── search/             # 搜索模块
│   ├── setting/            # 设置模块
│   ├── tag/                # 标签模块
│   ├── manage.py
│   └── requirements.txt
├── frontend/               # Next.js 前端
│   ├── app/                # App Router 页面
│   ├── components/         # React 组件
│   ├── lib/                # 工具库
│   └── package.json
├── nginx/                  # Nginx 配置
├── docker-compose.yml      # Docker 编排
└── .env                    # 环境变量
```

## 🔧 常用开发命令

### 数据库操作

```bash
# 启动数据库
docker compose up -d db

# 停止数据库
docker compose stop db

# 查看数据库日志
docker compose logs -f db

# 进入 MySQL 命令行
docker compose exec db mysql -u lenjoy -plenjoy123 lenjoy

# 完全清除数据库数据（谨慎！）
docker compose down -v
```

### Django 命令

```powershell
# 确保已激活虚拟环境
cd api

# 数据库迁移
python manage.py makemigrations    # 生成迁移文件
python manage.py migrate           # 执行迁移

# 创建超级用户
python manage.py createsuperuser

# 收集静态文件
python manage.py collectstatic

# 进入 Django Shell
python manage.py shell

# 运行测试
python manage.py test
```

### 前端命令

```powershell
cd frontend

# 开发模式
pnpm dev

# 构建生产版本
pnpm build

# 启动生产服务器
pnpm start

# 代码检查
pnpm lint
```

## 🐛 常见问题

### 1. 数据库连接失败

**问题：** `Can't connect to MySQL server on 'localhost'`

**解决：**
```bash
# 检查数据库容器状态
docker compose ps

# 如果状态不是 healthy，查看日志
docker compose logs db

# 重启数据库
docker compose restart db
```

### 2. 端口被占用

**问题：** `Error: listen EADDRINUSE: address already in use`

**解决：**
```powershell
# 查找占用端口的进程 (Windows)
netstat -ano | findstr :3000
netstat -ano | findstr :8000

# 结束进程
taskkill /PID <进程ID> /F
```

### 3. mysqlclient 安装失败

**问题：** Windows 上安装 `mysqlclient` 失败

**解决：**
1. 下载预编译的 wheel 文件：https://www.lfd.uci.edu/~gohlke/pythonlibs/#mysqlclient
2. 或使用 conda 安装：
   ```bash
   conda install mysqlclient
   ```

### 4. 虚拟环境激活失败 (PowerShell)

**问题：** `无法加载文件，因为在此系统上禁止运行脚本`

**解决：**
```powershell
# 以管理员身份运行 PowerShell，执行：
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 5. 前端 API 请求失败

**问题：** CORS 错误或 API 404

**检查：**
1. 确保后端服务正在运行
2. 检查 `frontend/.env.local` 中的 API 地址配置
3. 确保后端 `CORS_ALLOW_ALL_ORIGINS=True`

## 🔄 开发工作流

```
┌─────────────────────────────────────────────────────┐
│                    开发环境架构                       │
└─────────────────────────────────────────────────────┘

  浏览器
    │
    ├──────────────────┬──────────────────┐
    │                  │                  │
    ▼                  ▼                  ▼
┌─────────┐      ┌──────────┐      ┌──────────┐
│ Next.js │      │  Django  │      │  Django  │
│ :3000   │ ───► │  :8000   │ ───► │  Admin   │
│ (前端)  │ API  │  (后端)   │      │  后台    │
└─────────┘      └────┬─────┘      └──────────┘
                      │
                      ▼
                ┌──────────┐
                │  MySQL   │
                │  :3306   │
                │ (Docker) │
                └──────────┘
```

### 推荐的开发流程

1. **启动服务**：先启动 Docker 数据库，再启动后端，最后启动前端
2. **代码修改**：修改代码后自动热重载，无需重启
3. **数据库变更**：修改 models 后执行 `makemigrations` 和 `migrate`
4. **提交代码**：确保代码通过 lint 检查

## 📚 相关文档

- [API 文档](api/API_DOCUMENTATION.md)
- [API 测试指南](api/API_TESTING.md)
- [Docker 部署指南](DOCKER_USAGE.md)
- [快速开始](QUICKSTART.md)

---

> 💡 **提示**：开发过程中遇到问题，可以查看 `api/logs/` 目录下的日志文件。
